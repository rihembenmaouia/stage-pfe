# Copy this file to .env and fill in your values. Do NOT commit .env or hardcode credentials.

# MongoDB Atlas (required — no Docker Mongo)
MONGO_URI=mongodb+srv://<USER>:<PASSWORD>@cluster0.tojwjkt.mongodb.net/mareversation?retryWrites=true&w=majority&appName=Cluster0

# Server
PORT=3001
NODE_ENV=development

# Auth
JWT_SECRET=your-super-secret-jwt-key-change-in-production
REFRESH_TOKEN_SECRET=your-refresh-token-secret-change-in-production

# Frontend URL (for CORS and cookies)
FRONTEND_URL=http://localhost:5173
CORS_ORIGIN=http://localhost:5173
