# Copy to .env in project root. Do NOT commit .env or secrets.

# Backend API base URL (no trailing slash). Used for all /api/v1 requests.
# Development: http://localhost:3001
# Production: your backend URL (e.g. https://mareservtaion-backend.vercel.app)
VITE_API_BASE_URL=http://localhost:3001
